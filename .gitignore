node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# artefatti di build e cache (mai versionati)
release/
.vite/
*.dmg
*.blockmap

# video di riferimento per la taratura (F/N reali filmati sul Theta-Meter).
# Servono a TARARE, non a far girare l'app: 115 MB che in git non tornerebbero mai indietro.
public/theta-meter-real.mov/
